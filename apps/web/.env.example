# Baked into the client at build time — set this in Vercel Project Settings → Environment Variables.
# Must be your public API origin + /api/v1 (not localhost).
NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com/api/v1
